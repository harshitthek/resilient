# Dependabot configuration for Resilient repository
version: 2
updates:
  # Keep GitHub Actions up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Keep Python dependencies up to date
  - package-ecosystem: "pip"
    directory: "/scripts"
    schedule:
      interval: "weekly"
